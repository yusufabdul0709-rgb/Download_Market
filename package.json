{
  "name": "download-market",
  "version": "1.0.0",
  "private": true,
  "description": "Download Market – download videos, reels, posts and audio from YouTube & Instagram",
  "scripts": {
    "start": "npm run server:start",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:worker\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:worker": "npm run dev:worker --prefix backend",
    "server:start": "npm start --prefix backend",
    "build": "npm run build --prefix frontend",
    "install:frontend": "npm install --prefix frontend",
    "install:backend": "npm install --prefix backend",
    "install:all": "npm install && npm run install:frontend && npm run install:backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
