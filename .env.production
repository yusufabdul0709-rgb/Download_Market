# Frontend environment — Production
# For single-server deployment, leave empty (frontend is served from same origin as API)
# For separate deployment, set to your backend URL e.g. https://api.download-market.com
VITE_API_BASE_URL=
